[project]
name = "artefact-x-evaneos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.1.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
]
